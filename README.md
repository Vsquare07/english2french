I trained machine translation models to predict french translation of english sentences.
<br>
I trained CBOW word2vec and used the embeddings to train a simple encoder_decoder model `encoder_decoder.ipynb`, then added attention to it `encoder_decoder_attention.ipynb` and then finally trained a transformer model `transformer.ipynb`